
DB_HOST="localhost"  # Use "127.0.0.1" if localhost doesn’t work
DB_USER="root"  # Use the username you created (e.g., "app_user" or "root")
DB_PASSWORD="root"# Use the password for the user (e.g., the password you set for "app_user" or root)
DB_NAME="wild"  # Use the name of the database you created locally (match your project needs)
DB_PORT=3306  # Default MySQL port; confirm if different
JWT_SECRET=mysecretkey  # Keep this or update to a secure, random string